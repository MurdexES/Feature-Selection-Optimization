# This project does not require secrets. Pass paths through CLI arguments.
# Example only; the program does not automatically load this file.
DATA_PATH=/secure/server/path/masked_data.parquet
OUTPUT_DIR=/secure/server/path/mcts_outputs
